How to run:

wget http://download.dojotoolkit.org/release-1.5.0/dojo-release-1.5.0.tar.gz
and unzip it into ui/js/

rename and edit config file

Start hadcc with:
$ runhaskell Hadcc.hs


How to install:

$ runhaskell Setup.lhs configure --prefix=$HOME --user
$ runhaskell Setup.lhs build
$ runhaskell Setup.lhs install

